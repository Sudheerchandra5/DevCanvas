import React from "react";
import ReactDOM from "react-dom/client";
import CoreJavaCatalog from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CoreJavaCatalog />
  </React.StrictMode>,
);
